List of Sensors for <%= @customer.name %>:

<br>
<% for sensor_id in @customer.sensor_id %>
<%= sensor_id.sensor_id %>
<br>
<% end %>
<br>
<%= link_to "Update", {:id => params[:id],:action => 'update_customer'} %>

TODO: In this page we want to be able to add new temperature readings for this customer.